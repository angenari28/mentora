{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish src/Mentora.API/Mentora.API.csproj -c Release -o /app/publish"
  },
  "deploy": {
    "startCommand": "cd /app/publish && dotnet Mentora.API.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}